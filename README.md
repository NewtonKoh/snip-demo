# Bundle

This branch is **generated output** — do not edit by hand.

The `/scripts/build-bundle.mjs` script on the `main` branch automatically assembles this release: the backend server, built frontend UI, and CLI tool in a single deployable folder.

Run the build on `main`:
```bash
node scripts/build-bundle.mjs --push
```
